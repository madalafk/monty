#include "monty.h"
/**
 * f_stack - prints the stack top using a pointer to the stack head
 * @head: the head of the stack
 * @counter: line_number
 * Return: no return
*/
void f_stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}
